# MongoDB Configuration
# Replace with your MongoDB connection string
# Example: mongodb://localhost:27017/phantom-cleaning
# Or MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/phantom-cleaning
MONGO_URI=mongodb://localhost:27017/phantom-cleaning

# JWT Configuration
# Generate a strong random secret key for production
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Admin Panel Security
# Password required to access admin dashboard and sensitive features
ADMIN_PANEL_PASSWORD=your-admin-password-change-this

# Server Configuration
# Port number the server will run on
PORT=5050

# Environment
# Options: development, production, test
NODE_ENV=development
